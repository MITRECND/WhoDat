import React from 'react'
import DNSDB from './dnsdb'

const PassiveHander = (props) => {

    return (
        <React.Fragment>
            <DNSDB/>
        </React.Fragment>
    )
}

export default PassiveHander